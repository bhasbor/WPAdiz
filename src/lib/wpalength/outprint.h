#include <stdio.h>
#include "color.h"

void out_add_print(char* word, const unsigned length);
void out_err_print(char* word, const unsigned length);
void out_print_read_file(char* file, const unsigned long* charmax, const unsigned leng, unsigned* lengshort);
void out_print_cancell(unsigned* lengshort, unsigned long* charmax);
